# Example: Unified credentials in .env files
#
# Copy this to .env and fill in your values
# IMPORTANT: Never commit .env files to git!

# ============================================================================
# Git Authentication (GitHub PAT)
# ============================================================================

# GitHub Personal Access Token
# Generate at: https://github.com/settings/tokens
# Scopes needed: repo (for private repos), read:org (for org repos)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Alternative environment variable names (Kodama supports both)
# GH_TOKEN=ghp_your_github_personal_access_token_here

# ============================================================================
# Claude Code Authentication
# ============================================================================

# Claude Code Auth Token
# Obtain from: https://console.anthropic.com/settings/keys
CLAUDE_CODE_AUTH_TOKEN=sk-ant-api03-your_claude_api_key_here

# Alternative environment variable names
# ANTHROPIC_API_KEY=sk-ant-api03-your_claude_api_key_here

# ============================================================================
# Application Environment Variables
# ============================================================================

# Database
DATABASE_URL=postgresql://localhost:5432/mydb

# Redis
REDIS_URL=redis://localhost:6379

# API Endpoint
API_ENDPOINT=https://api.example.com

# Application settings
APP_NAME=myapp
APP_ENV=development
LOG_LEVEL=info

# ============================================================================
# How This Works
# ============================================================================

# When you start a Kodama session with this .env file:
#
# 1. Kodama reads the .env file from your LOCAL machine
# 2. All variables are stored in a Kubernetes secret
# 3. The secret is injected into the pod as environment variables
# 4. Both GitHub operations and Claude Code auth use these variables
#
# No need for separate:
# - K8s secrets for git credentials
# - K8s secrets for Claude auth
# - File-based authentication
#
# Everything is unified through .env files!

# ============================================================================
# Usage Examples
# ============================================================================

# Method 1: With .kodama.yaml template (auto-loads .env)
#   cd /path/to/repo
#   kubectl kodama start dev --sync .
#
# Method 2: CLI flag (override template)
#   kubectl kodama start dev --env-file .env --sync .
#
# Method 3: Multiple files (last wins for duplicates)
#   kubectl kodama start dev \
#     --env-file .env \
#     --env-file .env.local \
#     --sync .

# ============================================================================
# Security Notes
# ============================================================================

# ✓ System variables (PATH, HOME) are never overridden
# ✓ Kubernetes variables are protected
# ✓ Secrets are automatically cleaned up on session deletion
# ✓ Variable names validated (must be UPPERCASE_WITH_UNDERSCORES)
# ✗ DO NOT commit .env files to version control
# ✗ Add .env* to .gitignore
